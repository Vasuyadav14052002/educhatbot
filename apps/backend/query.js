const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const u = await prisma.user.findFirst({where: {email: 'rajesh.sharma@example.com'}}); const r = await prisma.parentStudentRelation.findMany({where: {parent_user_id: u.id}, include: {student: true}}); console.log('User:', u.id, 'Relations:', JSON.stringify(r)); } main().finally(() => prisma.$disconnect());
